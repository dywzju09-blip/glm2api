services:
  glm-studio:
    build: .
    container_name: glm-studio
    restart: unless-stopped
    ports:
      - "${GLM_PORT:-8000}:8000"
    environment:
      # 管理面板登录密钥（在部署机的 .env 里设置，不设置则首次启动自动生成）
      ADMIN_KEY: "${ADMIN_KEY:-}"
      PORT: "8000"
    volumes:
      - ./data:/app/data
