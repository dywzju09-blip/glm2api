# GLM Studio 环境变量样例（本地裸跑时复制为 .env 并按需修改；Docker 部署用 docker-compose.yml）

# 管理面板登录密钥。不设置时首次启动会自动生成随机密钥并打印到日志、同时存入数据库。
ADMIN_KEY=

# 监听地址与端口（VPS 公网访问保持 0.0.0.0）
HOST=0.0.0.0
PORT=8000

# 数据目录（SQLite 数据库存放位置）
DATA_DIR=data
